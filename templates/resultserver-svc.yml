apiVersion: v1
kind: Service
metadata:
  name: resultserver-svc
  labels:
    app: {{ .Values.resultserver-service.tag }}
spec:
  selector:
    app: {{ .Values.resultserver-service.tag }}
  type: {{ .Values.resultserver-service.type }}
  ports:
   - name: http
     port: {{ .Values.resultserver-service.port }}
     targetPort: {{ .Values.resultserver-service.targetport }}
     nodePort: {{ .Values.resultserver-service.nodePort }}
     protocol: TCP